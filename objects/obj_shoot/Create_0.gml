/// ===========================================
/// Create Event - obj_shoot
/// ===========================================

// posição inicial do tiro
x_start = x;
y_start = y;

// distância máxima que o tiro pode percorrer
max_distance = 100; // ajuste em pixels

// velocidade e direção já devem ser configuradas ao criar o tiro
// exemplo no player: speed = 14; direction = (image_xscale == 1 ? 0 : 180)
